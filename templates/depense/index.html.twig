{% extends 'back_base.html.twig' %}

{% block body %}
<div id="google_translate_element"></div>
<script type="text/javascript">
    function translateToFrench() {
        var el = document.getElementById("google_translate_element").querySelector(".goog-te-combo");
        el.value = "fr";
        el.dispatchEvent(new Event('change'));
    }

    function translateToEnglish() {
        var el = document.getElementById("google_translate_element").querySelector(".goog-te-combo");
        el.value = "en";
        el.dispatchEvent(new Event('change'));
    }
</script>
<script type="text/javascript" src="https://translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
<script type="text/javascript">
    function googleTranslateElementInit() {
        new google.translate.TranslateElement({pageLanguage: 'fr', includedLanguages: 'en', autoDisplay: false}, 'google_translate_element');
    }
</script>


    <h1>Liste des depenses</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                
                <th>Description</th>
                 <th>monatant</th>
                  <th>Date</th>

               
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for depense in depenses %}
            <tr>
                <td>{{ depense.id }}</td>
                <td>{{ depense.description }}</td>
               
                

                
                <td>{{ depense.montant }}</td>
                <td>{{ depense.date ? depense.date|date('Y-m-d') : ''  }}</td>
                
                
                <td>
                     <a class="btn btn-outline-info" style="width: 100px; " href="{{ path('app_depense_show', {'id':depense.id}) }}">Détails</a><br>
                    <a  class="btn btn-outline-primary" style="width: 100px; " href="{{ path('app_depense_edit', {'id': depense.id}) }}">modifier</a><br>
                     <a  class="btn btn-outline-danger" style="width: 100px; " onclick="return confirm('Are you sure you want to delete this item?')" href="{{ path('app_depense_delete', {'id': depense.id}) }}">supprimer</a><br>
                   
                  
    <!-- Vos champs de formulaire -->
    
</form>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div>


    <a class="btn btn-outline-success" style="width: 110px; " href="{{ path('app_depense_new') }}">Ajouter</a>
      
                 
{% endblock %}
